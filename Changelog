Major changes list:

1. Added Repository layer for logic related to accessing physical storage (as requested).
2. Changed approach: to generate a class and use SpecificDatumReader instead of GenericDatumReader (as requested).
3. Enlarged the schema with more fields of different types, a sample is in resources folder (optional task).
4. Added some unit/integration tests.


P.S. Thanks for really interesting task and reviewing!